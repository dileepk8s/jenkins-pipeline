/// this is first pipeline for SCM
pipeline {
  agent any 
  stages {
    stage ('build') {
      steps {
        echo "this is first pipeline"
      }
    }
    stage ('devstage') {
      steps {
        echo "this is dev stage"
      }
    }
  }
}
