pipeline {
    agent any 
    stages {
        stage ('build') {
            steps {
                // verify the maven version
                echo "welcome to maven section , verify the maven version"
                sh 'mvn --version'
            }
        }
    }
}
